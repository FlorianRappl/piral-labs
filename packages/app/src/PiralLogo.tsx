import * as React from "react";
import logo from "./images/logo.png";

const PiralLogo: React.FC = () => (
  <img src={logo} alt="Piral Logo" width={128} height={128} loading="lazy" />
);

export default PiralLogo;
